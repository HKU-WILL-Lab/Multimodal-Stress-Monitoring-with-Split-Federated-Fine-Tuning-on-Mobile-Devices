# Configuration policy

Publication-safe SFL examples are in `sfl_runtime/configs/`. Copy an example to
an ignored local runtime directory before inserting real server addresses,
asset paths, or device-specific settings. Never commit credentials, personal
filesystem paths, device serials, or private network addresses.
